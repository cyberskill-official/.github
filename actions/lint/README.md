# 🛠️ Lint

This action lints the project

## Inputs

| Name | Description | Required | Default |
| ---- | ----------- | -------- | ------- |
| `SKIP_YAML_LINT` | Skip the YAML lint step (default: false) | `false` | `false` |

## Usage

```yaml
uses: cyberskill-official/.github/actions/lint@main
with:
```
